/* global it, expect, describe */
import Sandpit from './Sandpit'

it('thinks Charlie is great', () => {
  expect(true)
})

describe('Sandpit', () => {
  it('should exist', () => {
    expect(Sandpit).toBe(Sandpit)
  })
})
